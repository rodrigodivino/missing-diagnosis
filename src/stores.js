import { writable } from "svelte/store";

// export const canvasWidth = writable(1000 * 1.8);
// export const canvasHeight = writable(600 * 1.5);

// Nullity 2-column
// export const canvasWidth = writable(600);
// export const canvasHeight = writable(600);

// Arcs to fit paper
export const canvasWidth = writable(1000 * 1.2);
export const canvasHeight = writable(600 * 1.5);
